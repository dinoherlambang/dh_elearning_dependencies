<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="slide_channel_view_form" model="ir.ui.view">
            <field name="name">slide.channel.form</field>
            <field name="model">slide.channel</field>
            <field name="inherit_id" ref="website_slides.slide_channel_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//div[@name='options']" position="inside">
                    <div>
                        <label for="prerequisite_ids" string="Prerequisites"/>
                        <field name="prerequisite_ids" widget="many2many_tags"/>
                    </div>
                </xpath>
            </field>
        </record>

        <record id="slide_channel_view_kanban" model="ir.ui.view">
            <field name="name">slide.channel.kanban</field>
            <field name="model">slide.channel</field>
            <field name="inherit_id" ref="website_slides.slide_channel_view_kanban"/>
            <field name="arch" type="xml">
                <field name="prerequisite_ids">
                    <tree>
                        <field name="name"/>
                    </tree>
                </field>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <!-- Existing kanban box content -->

                            <!-- Display prerequisite courses -->
                            <div class="oe_kanban_bottom_prerequisites" groups="base.group_user">
                                <span>Prerequisites:</span>
                                <field name="prerequisite_ids"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </field>
        </record>
    </data>
</odoo>
